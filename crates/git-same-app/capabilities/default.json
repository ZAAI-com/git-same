{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Git-Same window: core IPC plus event listen/emit for the status-updated stream.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
